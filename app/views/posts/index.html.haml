- @posts.each do |post|

	%h2= link_to post.title, post
	= image_tag(post.user.avatar.url(:thumb))
	%p= post.content
	%p 
		Number of comments:
		= post.comments.count
	%p
		Published at
		= time_ago_in_words(post.created_at)
		by
		= post.user.email